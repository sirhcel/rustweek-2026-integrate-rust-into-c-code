[package]
name = "rusty"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

[dependencies]

[build-dependencies]
cbindgen = "0.29.2"
